<h1>Director #<%= @the_director.id %> Details</h1>

<a href="/directors">Go back</a>

<hr>

<form action="/update_director/<%= @the_director.id %>" method="post">
  <label for="image_director">Image</label>
  <input type="text" id="image_director" name="image_director" value="<%= @the_director.image %>">

  <button type="submit">Update director</button>
</form>

<hr>

<dl>
  <dt>Name</dt>
  <dd><%= @the_director.name %></dd>

  <dt>Dob</dt>
  <dd><%= @the_director.dob %></dd>

  <dt>Bio</dt>
  <dd><%= @the_director.bio %></dd>

  <dt>Image</dt>
  <dd><img src="<%= @the_director.image %>" style="width:250px;"></dd>
</dl>

<hr>

<h2>Movies Directed</h2>
<ul>
  <% @movies.each do |movie| %>
    <li>
      <%= movie.title %> 
      (<a href="/movies/<%= movie.id %>">Show details</a>)
    </li>
  <% end %>
</ul>

