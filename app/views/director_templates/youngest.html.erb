<h1>Youngest Director</h1>

<% if @youngest.present? %>
  <p>
    The youngest director in our dataset is 
    <a href="/directors/<%= @youngest.id %>">
      <%= @youngest.name %>
    </a>, born on <%= @youngest.dob.strftime("%B %e, %Y") %>.
  </p>
<% else %>
  <p>No directors found in the database.</p>
<% end %>

